<div class="p-2">
  <h1 class="text-xl font-bold">Habits</h1>
  <div class="flex flex-col gap-2">
    <% @habits.each do |habit|%>
      <div>
        <h2 class="text-l"><%= link_to habit.name, habit %></h2>
      </div>
    <%end%>
  </div>
</div>
