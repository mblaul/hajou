<div class="py-4 flex flex-col gap-4">
  <div class="flex flex-row items-center justify-start gap-2">
    <%= render 'partials/page_header' do %>
      Habits
    <% end %>
    <%= render 'partials/button', locals: { variant: 'small' } do %>
      <%= link_to 'New Habit →', 'habits/new'%>
    <% end %>
  </div>
  <div class="w-full h-1 bg-gradient-to-r from-60% from-purple-600 to-orange-600"></div>
  <div class="flex flex-col gap-2">
    <% @habits.each do |habit|%>
      <%= render partial: 'partials/card', locals: { item: habit } %>
    <% end %>
  </div>
</div>
